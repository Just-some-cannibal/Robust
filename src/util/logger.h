#pragma once

#include <iostream>
#include <fstream>
#include <chrono>
#include <string>
#include <chrono>
#include <iomanip>
#include <ctime>

namespace robust {
namespace util {
  void log (std::string log);
  void err (std::string log);
}
}